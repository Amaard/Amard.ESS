name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: "9.x"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Amard.ESS

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore Amard.ESS.sln

      - name: Build
        run: dotnet build Amard.ESS.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test Amard.ESS.sln --configuration Release --no-restore --no-build

      - name: Publish
        run: dotnet publish Amard.ESS.sln --configuration Release --no-restore --no-build
